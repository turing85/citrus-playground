test-config.sut.url = ${SUT_URL:http://localhost:8080}
test-config.artemis.url = ${ARTEMIS_URL:tcp://localhost:61616}
test-config.artemis.username = ${ARTEMIS_USERNAME:artemis}
test-config.artemis.password = ${ARTEMIS_PASSWORD:artemis}
test-config.artemis.receive.address = ${JMS_TOPIC:messages}
test-config.artemis.receive.queue = ${JMS_RECEIVE_QUEUE:citrus}
test-config.artemis.receive.fqqn = ${test-config.artemis.receive.address}::${test-config.artemis.receive.queue}
